version: v1
managed:
  enabled: true
  go_package_prefix:
    default: github.com/d3c3ptive/aims
    except:
      - buf.build/googleapis/googleapis

plugins:
  - name: go-grpc
    out: .
    opt: paths=source_relative
